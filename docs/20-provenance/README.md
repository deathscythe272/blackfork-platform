# Provenance — Docs In Progress

The running slice is documented in `v1-slice.md`: one agent, the gateway, the evidence
server, guardrails, three evals, and a trace, all local. Design for the full system
lives in `docs/02-architecture/` (flow + narrative). This section will grow one
template doc per plane — data, context, agent, assurance — plus the Pipeline Steward
(chunks P1–P5). Build order: `../ROADMAP.md`, phase 7.
