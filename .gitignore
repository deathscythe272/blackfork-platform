# terraform: state, providers, and anything that names a real project
*.tfstate
*.tfstate.*
.terraform/
*.tfvars
!*.tfvars.example
backend.hcl
*.tfplan
# python
__pycache__/
*.py[cod]
.venv/
.venv-data/
# node
node_modules/
# env & os
.env
.env.*
.DS_Store
# provenance slice: generated data, logs, traces, local env
data/*.duckdb
data/warehouse/
data/catalog.db
dagster_home/
audit/
jobs/
traces/
.env
*.log
*.egg-info/
/profiling/

# the scanner's raw reports carry prompts and answers; they ride with the workflow run, not the repo
src/provenance/evals/results/redteam-*.report.jsonl
