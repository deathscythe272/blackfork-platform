# The data plane: pipeline, lakehouse, redaction. Its own environment and image, because
# the redaction library and the agent toolkit disagree on shared dependencies.
dagster==1.13.21
dagster-webserver==1.13.21
pyiceberg[gcsfs,duckdb,sql-sqlite,pyarrow]==0.12.0
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
duckdb==1.5.5
PyYAML>=6
