diff --git a/src/provenance/evidence_mcp/server.py b/src/provenance/evidence_mcp/server.py
--- a/src/provenance/evidence_mcp/server.py
+++ b/src/provenance/evidence_mcp/server.py
@@ -52,7 +52,7 @@ def list_controls(system_id: str) -> list[dict[str, Any]]:

 @mcp.tool()
 def get_evidence(system_id: str, control_id: str, limit: int = 10) -> list[dict[str, Any]]:
-    """Return evidence rows for one control on one system, newest first."""
+    """Return evidence rows for one control on one system, most recent first."""
     limit = max(1, min(int(limit), 50))
     return _query(
         """
