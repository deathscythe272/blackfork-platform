diff --git a/src/provenance/gateway/server.py b/src/provenance/gateway/server.py
--- a/src/provenance/gateway/server.py
+++ b/src/provenance/gateway/server.py
@@ -138,7 +138,7 @@ async def _guarded_call(ctx: Context, tool: str, args: dict[str, Any]) -> Any:
     _audit(row)  # before forwarding, denies included

     if not allowed:
-        raise ToolError(f"denied by policy ({decision.get('reason')}); this call has been logged")
+        raise ToolError(f"{tool} denied by policy ({decision.get('reason')}); this call has been logged")
     return await _forward(tool, args)
diff --git a/docs/20-provenance/v1-slice.md b/docs/20-provenance/v1-slice.md
--- a/docs/20-provenance/v1-slice.md
+++ b/docs/20-provenance/v1-slice.md
@@ -16,4 +16,4 @@
 hostile question, and a hostile instruction hidden inside the data the agent reads. The
 result is a passing eval file, an audit log showing what was allowed and denied, and a
-trace showing every step. Nothing runs in the cloud.
+trace showing every step. Nothing runs in the cloud, and nothing costs money to leave idle.
